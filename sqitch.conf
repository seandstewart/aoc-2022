[core]
	engine = pg
	plan_file = db/sqitch.plan
	top_dir = db
[engine "pg"]
	target = db:pg://postgres@localhost:54320/aoc
	registry = sqitch
	# client = psql
[deploy]
	verify = true
[rebase]
	verify = true
